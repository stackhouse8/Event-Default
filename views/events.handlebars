{{#if loggedIn}}
{{#each events as |event|}}
<section class="events">
  <div class="events">
    <h3 id="eventName">Name: {{event.event_name}}</h3>
    <p id="eventDate">Date: {{event.event_date}}</p>
    <p id="eventLoco">Location: {{event.event_location}}</p>
    <p id="eventDesc">Description: {{event.event_description}}</p>
  </div>
</section>
{{/each}}
{{else}}
<a href="/login">You must login first to view this page</a>
{{/if}}
