const eventHandler = async (event) => {
    event.preventDefault();

    const eventName = document.querySelector("eventName")
    const eventDate = document.querySelector("eventName")
    const eventLocation = document.querySelector("eventName")
    const eventDescription = document.querySelector("eventName")

    if (loggedIn) {
        const render = await fetch('')
    }

}
